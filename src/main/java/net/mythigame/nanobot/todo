Mettre en place des EMBED pour les gifs (pour permettre le event.reply directement).
Faire le système de statistiques (commandCount n+1d, n+2d, n+3d, n+4d, n+5d, n+6d, n+7d, n+1mo, n+2mo, n+3mo, n+1y

-- MUSIQUES
Pour les votes faire en sorte de ne pas compter les bots

giveaway --
-- UTILES > SendMessage on command with give on conditionComplete

-- CACHE
Faire un système de cache redis comme CommonsMGC avec les guilds pour mieux tout gérer

-- SERVICE
/service | Permet de prendre sont service
/services setup "name", "channelAllow", "logsChannel", "role", "optionalrole" | Permet de mettre en place un nouveau salon de prise de service
/services add "user", "time" | Permet d'ajouter du temps de service à une personne
/services remove "user", "time" | Permet de retirer du temps de service à une personne
/services tableau | Permet d'obtenir un tableau des heures de toutes les personnes
/services clear | Permet de reset les services d'un tableau

-- MODERATION
ban --> Rajouter une option SQL pour case
tempban --> Ban temporaire --> if(time > timeend) unban(User) | Option SQL pour case
unban --> Rajouter une option SQL pour case
kick --> Kick | Option SQL pour case
mute --> Permet de mute message & vocal (Créer un rôle "muted" --> addpermission on vocalChannel and TextChannel) | Option SQL pour case
tempmute --> tempmute | Option SQL pour case
unmute --> unmute | Option SQL pour case
suppr --> int or messageId (between firstMessageId and SecondMessageId all message between messages --> delete();)
votegame --> Faire en sorte qu'au start du bot, toute les réactions des votegame soient supprimés.

-- Unique via SQL
Faire en sorte que chaque Guild puisse custom le prefix du bot (SQL requis)



TODO :
userCommands.fun
userCommands.utilsCommands && moderationCommands.utilsCommands
moderationCommands (remake et add mute, tempmute, unmute, suppr & modify actual commands to add case support)
userCommands.musicCommands (remake les messages, fait des embed avec des réactions pour gérer la musique)